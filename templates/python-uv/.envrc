# shellcheck shell=bash
# Re-run the shellHook (uv sync) when dependencies change, not only when the
# flake changes. watch_file must precede `use flake` (nix-direnv README).
watch_file pyproject.toml uv.lock
use flake
