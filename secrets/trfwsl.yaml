# Placeholder — replace with `sops secrets/trfwsl.yaml` after bootstrap.
# Required keys: cloudflared/tunnel-token, mullvad/account,
# minecraft/rcon-password, recyclarr/sonarr-api-key, recyclarr/radarr-api-key
{}
